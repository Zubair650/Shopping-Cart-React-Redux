// import React from 'react';

const Home = () => {
    return (
        <div>
            <h2>This is a simple CRUD operation for Book Store using React and Redux Toolkit</h2>
        </div>
    );
};

export default Home;